show_debug_message("### oPositionButton.create");

// Référence vers la carte parente (sera définie par UIManager)
parentCard = noone;

// Visibilité du bouton
visible = true;
image_alpha = 0.8;

// Échelle du bouton
image_xscale = 0.5;
image_yscale = 0.5;

// Profondeur pour être au-dessus des cartes
depth = -2000;